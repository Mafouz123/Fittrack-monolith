services:
  - type: web
    name: mafouz-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    autoDeploy: true

services:
  - type: web
    name: fittrack-monolith
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app

    startCommand: streamlit run main.py
    
